import 'package:flutter/cupertino.dart';

class StudentItemWidget extends StatelessWidget {
  const StudentItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
